name: Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Run Slack Notifier Action
      id: slack-notifier
      uses: ../action.yml@latest
      with:
        channel: test
